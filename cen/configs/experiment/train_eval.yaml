train:
  loss: ???
  batch_size: ???
  epochs: ???
  checkpoint_kwargs:
    monitor: "val_loss"
    save_best_only: true
    verbose: true
  tensorboard: true
  verbose: 1

eval:
  batch_size: ${train.batch_size}
  metrics: ???
  verbose: 1
